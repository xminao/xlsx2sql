CREATE TABLE [schema_user].[table_name] (
    [column_name] [data_type] [nullable],
    -- Add primary key constraint if db_pk is 'Y'
    [column_name] [data_type] PRIMARY KEY [nullable],
    -- Add column comments
    -- column_comment
);